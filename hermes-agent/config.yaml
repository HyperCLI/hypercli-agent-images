_config_version: 33

model:
  provider: hypercli
  default: ${env:HERMES_DEFAULT_MODEL}

providers:
  hypercli:
    name: HyperCLI
    api: ${env:HYPER_AGENTS_API_BASE}
    key_env: HYPER_AGENTS_API_KEY
    transport: ${env:HERMES_MODEL_TRANSPORT}
    default_model: ${env:HERMES_DEFAULT_MODEL}
